package exam;

public class Example01 {
	/*
	 * 상속
	 * - 부모 클래스가 가진 기능을 자식클래스에서 사용할 수 있다.
	 * - 상속해 주는 클래스는 : 부모클래스, 상위클래스, 슈퍼클래스
	 * - 상속받는 클래스는 : 자식클래스, 하위클래스, 서브 클래스
	 * - 자식클래스를 선언할 때 extends 라는 키워드를 사용해서 상속받을 
	 * 클래스를 지정
	 * - c++, python 과는 달리 자바에서는 클래스간에 다중상속을 불허함
	 * 단 인터페이스 다중상속은 가능
	 * 
	 * 형식)
	 * class A {
	 * 		// 부모클래스
	 * }
	 * 
	 * class B extends A {
	 * 		// 자식클래스
	 * 		// B클래스는 A클래스를 상속받음
	 * 		// A클래스의 자식클래스
	 */
}
