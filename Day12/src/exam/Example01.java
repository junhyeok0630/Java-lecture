package exam;

public class Example01 {
	/*
	 * 접근 제한자 (public, default, protected, private)
	 * - 말 그대로 접근을 제한한다.
	 * 즉) 클래스 및 인터페이스가 가지고 있는 멤버의 접근을 말함.
	 * 
	 * public
	 * - 어디서든 자유롭게 접근 가능
	 * 
	 * protected
	 * - 같은 패키지 또는 자식 클래스에서 접근 가능
	 * 
	 * private
	 * - 외부에서 해당 멤버에 접근하지 못함
	 * 
	 * default
	 * - 같은 패키지에 소속된 클래스에서만 접근 가능
	 */
	
}
